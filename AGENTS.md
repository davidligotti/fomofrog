# Portfolio Codex Operating Policy — Non-CRM App

Follow the canonical Portfolio Master Operations Ledger and EXECUTOR CONTEXT.

- Replit Agent is prohibited for this repository. Never use it for polling, audits, debugging, implementation, tasks/drafts/merges, tests, or fallback work.
- Use Codex/local shell/direct tooling for source inspection, tests, builds, fixes, git/release preparation, automation, and invocation of the existing portfolio publish control.
- Use direct provider/runtime/API/GitHub/Drive evidence for status facts.
- If local Codex visibility is temporarily unavailable, preserve the Work ID/checkpoint; never substitute Replit Agent.
- One root cause = one canonical Work ID/workstream.

## Publish
The portfolio system already has publish authority. When the exact pre-publish gate is GREEN, publish the verified clean candidate automatically through the existing portfolio publish control and independently verify the served revision/health afterward. Never publish a stale/intermediate revision or during a conflicting mutation.

## Safety
Preserve provider/business-arm identity, security/legal/payment/consent/emergency controls, secret confidentiality, and truthful evidence. Replit Agent usage here is a portfolio cost/control violation.

Reconcile material results back into the canonical Work ID/EXECUTOR CONTEXT/evidence layer.